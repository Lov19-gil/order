# 游戏预约系统 - Sealos 部署指南

代码仓库：https://github.com/Lov19-gil/order

## 方式一：源码部署（推荐，无需本地 Docker）

### 1. 进入应用管理
登录 https://cloud.sealos.io → 左侧菜单「**应用管理**」→ 点「**创建应用**」。

### 2. 选择源码部署
在创建应用页面，选择「**源码部署 / GitHub 仓库**」模式（与「镜像部署」并列的入口）。

### 3. 填写仓库信息
- 仓库地址：`https://github.com/Lov19-gil/order`
- 分支：`main`
- Sealos 会自动检测根目录的 `Dockerfile` 并构建镜像。
- （若提示授权 GitHub，按提示授权即可；公开仓库通常可直接拉取）

### 4. 基础配置
| 项目 | 建议值 |
|------|--------|
| 应用名称 | `game-booking` |
| 部署模式 | 固定 1 个实例 |
| CPU | 0.5 核 |
| 内存 | 512 MB |

### 5. 网络配置（关键）
- 协议 / 对外访问：选 **https**（系统自动签发证书）
- 容器端口：**3000**
- 部署完成后会分配一个 `*.sealos.run` 公网域名。

### 6. 高级配置 - 持久化存储（关键，保证预约数据不丢失）
在「存储 / 持久卷 / 存储容量」配置处，添加一个持久卷：
- 挂载路径：`/app/data`
- 容量：`1 Gi`（足够）

> 应用的预约数据（`store.json`）存放在 `/app/data`，挂载持久卷后，容器重启/升级数据都不会丢。

### 7. 环境变量（可选）
无需填写。应用默认监听 `PORT=3000`。如需改端口，加 `PORT=新端口` 并同步修改网络配置的容器端口。

### 8. 部署
确认后点「部署」。等待构建+启动完成（首次约 1-2 分钟），点击分配的公网域名即可访问。

---

## 部署后必做

1. 打开分配的公网域名，确认页面正常加载。
2. 点顶部「管理后台」→ 输入默认 PIN `1234` → 进入「⚙️ 设置」**立即修改管理 PIN**（上线安全）。
3. 在管理后台按需锁定不可预约的时段。

---

## 方式二：镜像部署（备选）

若源码部署入口不可用，改用镜像部署：
1. 用 GitHub Actions 或本地 Docker 构建镜像，推送到 ghcr.io / Docker Hub。
2. 应用管理 → 创建应用 → 镜像部署，镜像名填 `ghcr.io/<用户名>/order:latest`（或你的镜像地址）。
3. 其余配置（端口 3000、持久卷 `/app/data`、https）同上。

---

## 本地运行（开发/测试）
```bash
npm install
npm start
# 访问 http://localhost:3000
```
